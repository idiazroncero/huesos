<header class="pen-header">
    <h1 class="m0">
        <a href="{{ previewUrl }}" title="Component preview">
            {{ entity.title }}
        </a>
    </h1>
    {{ status.tag(entity.status) }}
</header>
